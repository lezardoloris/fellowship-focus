# Monorepo root — set Railway Root Directory = "web" (uses web/railpack.json).

[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm run start --prefix web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
